<%= form_with(model: invoice_state, local: true) do |form| %>
  <% if invoice_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invoice_state.errors.count, "error") %> prohibited this invoice_state from being saved:</h2>

      <ul>
      <% invoice_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Name %>
    <%= form.text_field :Name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
